import { createContext } from 'react';


//-- Initial state
const initialState = {};

//-- Create contexts
export const RGComponentContext = createContext(initialState);
export const RGExample1Context = createContext(initialState);
export const RGExample2Context = createContext(initialState);
export const RGExample3Context = createContext(initialState);